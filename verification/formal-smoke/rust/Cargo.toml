[package]
name = "formal-smoke-model"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "formal_smoke_model"
path = "src/lib.rs"

[workspace]

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ["cfg(kani)"] }
