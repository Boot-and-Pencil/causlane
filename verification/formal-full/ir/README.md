# Generated Formal IR

`verification/formal-full/ir/generated/*.formal_ir.json` is produced by `just verification-full`.

The IR is a deterministic projection of compiled bundle and scenario facts.
Generated target artifacts and receipts must bind the same `formal_ir_hash`.
