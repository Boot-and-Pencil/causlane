# H1199 typed project-policy delta

The candidate keeps Causlane independent of Hopium products and replaces copied
policy implementations with the existing generic CLI Checker project profile.
Causlane retains repository-policy ownership; CLI Checker becomes the single
implementation owner for dependency, changed-file ownership, clean-break, and
assurance-lane validation. Required formal, property, and fuzz canaries remain
executable, and no native or formal check is removed.
