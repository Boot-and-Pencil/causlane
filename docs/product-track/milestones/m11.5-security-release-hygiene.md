# M11.5 — Security/release hygiene

- **Stage:** S11 — Public pre-alpha/bootstrap and alpha publication
- **Status:** `exists_harden`
- **Purpose:** licenses, dependency audit, context-pack scan, secret rules, vulnerability policy.

## Current evidence

- `deny.toml` defines the workspace advisory, license, ban and source policy.
- `./tools/cargo-dev deny check` is part of the pre-publication hygiene
  evidence and currently passes without advisory suppressions.
- The Restate runtime path uses the `aws_lc_rs` crypto backend instead of the
  RustCrypto RSA path that previously reached `RUSTSEC-2023-0071`.
- YAML-facing crates use `noyalib 0.0.8` with `compat-serde-yaml` instead of the
  deprecated `serde_yaml 0.9.34+deprecated` / `unsafe-libyaml` boundary. The
  workspace declared MSRV is therefore `1.85`.
- Replay parse-boundary tests preserve full-width `u64` coverage for JSON and
  explicitly reject float-backed large unsigned YAML literals instead of
  accepting rounded values.
- PUB4 evidence records the curated public baseline, Gitleaks handoff,
  branch-protection requirements and context-pack scan.
- PUB5 evidence records package file-list inspection and the passed
  `causlane-core` dry-run.

## Confirmed follow-up backlog

- Reduce or explicitly accept `cargo-deny` duplicate-version warnings before
  a full workspace `0.0.1` publication.
- Keep any future advisory/license/source exceptions narrow, dated and tied to
  release evidence rather than broad global suppressions.

## Required outputs

- normative doc/spec update;
- machine-readable artifact where applicable;
- tests/replay/formal receipt where applicable;
- coverage/readiness status update where applicable;
- risk/limitation note if not fully enforced;

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [ ] No protocol-critical behavior is prose-only.
- [ ] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.
