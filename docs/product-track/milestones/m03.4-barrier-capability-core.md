# M03.4 — Barrier/capability core

- **Stage:** S03 — Reference kernel и executable semantics
- **Status:** `done_or_near_done`
- **Purpose:** ExecutionBarrier, ExecutionCapability, scoped execution permission, executor guard.

## Required outputs

- normative doc/spec update;
- machine-readable artifact where applicable;
- tests/replay/formal receipt where applicable;
- coverage/readiness status update where applicable;
- risk/limitation note if not fully enforced;

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.

## Evidence

- Core primitives: `ExecutionBarrier` / `ExecutionCapability` with
  `derive_from_barrier`, `validate_for_barrier`, `canonical_capability_id` and
  `attestation_message` (`crates/causlane-core/src/domain/capability.rs`).
- Enforcement seam (ADR-0013): `ExecutorPort` requires `&ExecutionCapability`
  (`crates/causlane-core/src/application/ports.rs`); `GuardedExecutor::spend_barrier`
  authorizes deny-by-default *before* deriving/spending the capability
  (`crates/causlane-runtime/src/guarded_executor.rs`); replay validates spent
  capabilities through `KernelContracts` (`crates/causlane-replay/src/lib.rs`).
- Negative controls: every `ExecutionCapabilityError` variant now has a dedicated
  unit test (`OpNotCovered`, `LeaseCoverageMissing`, `BarrierMismatch`,
  `BindingMismatch` ×4 binding factors, `LeaseNotInBarrier`, `CapabilityIdMismatch`),
  plus canonical-id scoping and attestation-message determinism/injectivity tests in
  `capability.rs` — closing the S03 exit-gate "unit/property/Kani" requirement for
  the barrier/capability core.
