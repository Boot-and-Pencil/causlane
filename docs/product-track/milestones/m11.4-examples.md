# M11.4 — Examples

- **Stage:** S11 — Public pre-alpha/bootstrap and alpha publication
- **Status:** `done_or_near_done`
- **Purpose:** simple-local, approval-gate, consequence-parallelism and why-not-parallel are runnable and checked.

## Required outputs

- normative doc/spec update;
- machine-readable artifact where applicable;
- tests/replay/formal receipt where applicable;
- coverage/readiness status update where applicable;
- risk/limitation note if not fully enforced;

## Current evidence

- `examples/simple-local` is a standalone runnable example outside the
  publication workspace.
- `examples/approval-gate` is a standalone runnable example outside the
  publication workspace.
- `examples/consequence-parallelism` is a standalone runnable example outside
  the publication workspace.
- `examples/why-not-parallel` is a standalone runnable example outside the
  publication workspace.
- `tools/examples-check` runs format, check, clippy, tests and the binary.
- The example verifies a local audit trail through `causlane_replay::verify_events`.
- The negative control removes `ExecutionBarrierLogged` and expects
  `ReplayError::ExecutionWithoutBarrier`.
- The approval-gate example checks missing, self, weak, stale, wrong-plan and
  deny-wins approval outcomes, then verifies/refutes bundle-bound replay
  scenarios for approval witness binding.
- The consequence-parallelism example checks conflict-free frontier selection,
  pending write conflicts and lane capacity, then verifies/refutes
  bundle-bound replay scenarios for disjoint vs overlapping exclusive leases.
- The why-not-parallel example checks disjoint ready ops, pending write-scope
  conflict, unmet fact dependency and active writer blockers through typed
  `PairConflict` / `NotParallelReason` evidence.

## Remaining planned examples

- None.

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.
