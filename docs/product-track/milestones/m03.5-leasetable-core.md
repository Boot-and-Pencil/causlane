# M03.5 — LeaseTable core

- **Stage:** S03 — Reference kernel и executable semantics
- **Status:** `done_or_near_done`
- **Purpose:** Exclusive/shared/token leases, no-overlap, expiry/revocation, claim coverage.

## Required outputs

- normative doc/spec update;
- machine-readable artifact where applicable;
- tests/replay/formal receipt where applicable;
- coverage/readiness status update where applicable;
- risk/limitation note if not fully enforced;

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.

## Evidence

- `LeaseTable` (`crates/causlane-core/src/domain/constraint.rs`):
  grant/release, `validate_barrier_leases` (holder/epoch/expiry),
  `validate_claim_coverage`, plus `claim_modes_conflict` / `lease_covers_claim`; and
  `DrainFenceCheck` (`crates/causlane-core/src/domain/drain.rs`) for I-007.
- Tests: 8 LeaseTable unit tests (conflict, claim coverage, holder/epoch/expiry,
  mergeable-scope) + the drain fence test, and Kani harnesses
  `lease_conflict_rule_is_fail_closed_without_verified_merge` /
  `drain_fence_acquirable_only_without_active_overlap_nondet` (I-006/I-007). Replay
  negative controls: `conflicting_leases_invalid`, `drain_with_active_lease_invalid`.
