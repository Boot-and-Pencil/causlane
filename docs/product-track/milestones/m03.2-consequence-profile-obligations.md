# M03.2 — Consequence profile obligations

- **Stage:** S03 — Reference kernel и executable semantics
- **Status:** `done_or_near_done`
- **Purpose:** RuntimeExecution/ProjectionRead/Oversight/Topology/Evidence obligations in code and bundle.

## Required outputs

- normative doc/spec update;
- machine-readable artifact where applicable;
- tests/replay/formal receipt where applicable;
- coverage/readiness status update where applicable;
- risk/limitation note if not fully enforced;

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.

## Evidence

- `ConsequenceProfile` (`crates/causlane-core/src/domain/action.rs`) +
  `lifecycle_class_for_profile` / `route_consistent_with_profile`
  (`crates/causlane-core/src/domain/routing.rs`): the route's lifecycle class is
  derived from the profile, not chosen freely (I-005).
- Tests: `route_is_allowed_only_for_the_profiles_class` (routing.rs) + Kani
  `route_is_allowed_only_for_matching_profile_nondet`. Per-profile obligations are
  recorded in `verification/formal-full/obligations/lifecycle_product_obligations.yaml`; the bundle
  compiler delegates to the same core rule (`route_consistent_with_profile`).
