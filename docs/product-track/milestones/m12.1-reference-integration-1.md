# M12.1 — Reference integration 1

- **Stage:** S12 — Beta integrations и ecosystem validation
- **Status:** `done_or_near_done`
- **Purpose:** Rust service with API+worker+audit+projection.

## Evidence

`examples/reference-integration` is a standalone in-repo reference integration
slice. It submits host tasks through host-dispatch v2, drains them with the
linear runtime worker, appends audit events through the runtime audit adapter and
authorizes/redacts a projection read through the guarded projection helper.

This is a hardening slice, not a production service claim: durable queues,
distributed lease coordination, HTTP/database integration and retry policy remain
outside this executable example.

`docs/product-track/reference-integration-matrix.json` records the bounded gate
evidence and deferred claims; `tools/reference-integration-check` validates that
ledger and is wired into CI repository gates.

## Required outputs

- normative doc/spec update;
- machine-readable artifact where applicable;
- tests/replay/formal receipt where applicable;
- coverage/readiness status update where applicable;
- risk/limitation note if not fully enforced;

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.
