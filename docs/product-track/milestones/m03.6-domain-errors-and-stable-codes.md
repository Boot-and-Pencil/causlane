# M03.6 — Domain errors and stable codes

- **Stage:** S03 — Reference kernel и executable semantics
- **Status:** `done_or_near_done`
- **Purpose:** Typed errors with stable codes for replay/explain/CLI/tests.

## Required outputs

- normative doc/spec update;
- machine-readable artifact where applicable;
- tests/replay/formal receipt where applicable;
- coverage/readiness status update where applicable;
- risk/limitation note if not fully enforced;

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.

## Evidence

- Typed domain errors: `PlanHashError` (`action.rs`), `LeaseTableError`
  (`constraint.rs`), `ExecutionCapabilityError` (`capability.rs`).
- Stable codes: `ReplayError` + `ReplayErrorCode` with `code()` / `code_token()` /
  `as_str()` (`crates/causlane-replay/src/error.rs`); domain errors convert into the
  stable replay codes used by replay/explain/CLI.
- Tests/controls: each error path is unit-tested (PlanHash, lease, capability), and
  the stable codes back every replay negative control — the coverage report matches
  `expected_error_code` to `actual_error_code` per scenario.
