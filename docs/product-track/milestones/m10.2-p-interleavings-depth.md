# M10.2 — P interleavings depth

- **Stage:** S10 — Formal depth и proof hardening
- **Status:** `exists_expand`
- **Purpose:** Bounded race models for retry, cancellation, authz revocation, constraint epoch, worker partition.

## Required outputs

- normative doc/spec update: ADR-0027 records the P-first bootstrap boundary;
- machine-readable artifact where applicable: generated P payload now carries
  execution key, authz stage and lease epoch;
- tests/replay/formal receipt where applicable: `tools/formal-verify-all` runs
  three P-only controls for retry duplicate execution, authz revocation and
  stale constraint epoch;
- coverage/readiness status update where applicable: M10.2 moves to
  `exists_expand`, while planned ids remain outside active coverage;
- risk/limitation note: cancellation/supersession and worker partition recovery
  remain planned; `I-011..I-020` still receive no coverage credit;

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.

## Evidence

- P payload generation includes `executionKey`, `authzStage` and `leaseEpoch`.
- P monitors include `NoDuplicateHardExecutionForSameIdempotencyKey`,
  `AuthzRevocationBeforeBarrierBlocksExecution` and
  `NoStaleConstraintEpochAdmission`.
- P-only controls live in `contracts/scenarios/p_controls/` and are explicitly
  refuted by `tools/formal-verify-all`.
- Obligation manifest records P evidence hooks for `I-012`, `I-014` and `I-018`
  as planned work only; active coverage remains `I-001..I-010`.
