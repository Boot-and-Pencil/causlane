# M07.7 — Cookbook docs

- **Stage:** S07 — Observability, explainability и DX
- **Status:** `done_or_near_done`
- **Purpose:** Add action, approval, conflict, drain, replay, adapter, authz, projection recipes.

## Outcome

M07.7 adds [`docs/scenarios/cookbook.md`](../../scenarios/cookbook.md), a
fixture-backed recipe set for the current executable DX surfaces:

- action/predicate authoring;
- approval and witness failures;
- write-scope conflict and `why-not-parallel`;
- drain fences and active leases;
- replay explain and contract tests;
- authz success and negative controls;
- projection/read-sidecar and redaction checks;
- support-bundle build;
- adapter/runtime boundary checks.

The cookbook references existing commands, scenario fixtures and registries
rather than defining new protocol behavior.

## Required outputs

- normative doc/spec update: cookbook page plus docs index link;
- machine-readable artifact where applicable: existing scenario, graph and support-bundle fixtures/commands are referenced;
- tests/replay/formal receipt where applicable: documented commands are smoke-checked against existing fixtures;
- coverage/readiness status update where applicable: product-track status moves S07 to `advanced_in_repo` and S08 to `active_next`;
- risk/limitation note if not fully enforced: adapter certification remains in S08.

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.
