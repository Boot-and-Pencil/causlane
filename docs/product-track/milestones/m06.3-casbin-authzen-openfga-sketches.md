# M06.3 — Casbin/AuthZEN/OpenFGA sketches

- **Stage:** S06 — AuthZ, RBAC/ABAC/ReBAC, approvals и capabilities
- **Status:** `done_or_near_done`
- **Purpose:** Adapter contracts; do not bake one engine into core.

## Required outputs

- normative doc/spec update;
- machine-readable artifact where applicable;
- tests/replay/formal receipt where applicable;
- coverage/readiness status update where applicable;
- risk/limitation note if not fully enforced;

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.

## Progress (2026-06-22)

The M06.2 Cedar mapping is generalized into one engine-agnostic adapter contract
plus three lean per-engine request-shape sketches. Mapping-only, dependency-free,
decision-neutral — no engine in `causlane-core` **or** in `causlane-runtime`
(`Cargo.lock` unchanged); the deny-by-default gate (`authz_gate`) stays the sole
authority.

- `crates/causlane-runtime/src/adapters/engine.rs` (new): the contract —
  `HostAuthzRequest { call: &ActionCall, plan, stage }` (the single lingua franca,
  borrowing `ActionCall`) and `trait AuthzEngineAdapter { type Request; engine();
  map_request(&HostAuthzRequest) -> Self::Request }`. No `Error` type (mapping is
  total/infallible); `engine()` is a diagnostics-only label, never a dispatch key.
- `cedar.rs` refactored (alias-preserving, zero churn): `CedarAuthzRequest` is now
  a type alias of `HostAuthzRequest`; `CedarAdapter` implements the trait; M06.2's
  types/`to_cedar`/tests are unchanged.
- Three sketches (`casbin.rs`, `authzen.rs`, `openfga.rs`) — each an
  engine-shaped Causlane-owned request type + `to_*` + adapter impl:

  | Causlane | Cedar | Casbin | AuthZEN | OpenFGA |
  |---|---|---|---|---|
  | `subject_ref` | Principal | `sub` | `subject` | `object.id` (instance) |
  | `action_id` | Action | `act` | `action.name` | `relation` |
  | `predicate` | Resource | `obj` | `resource` | `object.type` |
  | `circumstance_ref` | Context | context | context | `user` (actor) |
  | plan + stage | Context | context | context | `binding` sidecar |

  **Modeling choices (documented, not guessed):** OpenFGA follows ReBAC semantics —
  `subject_ref` is "the subject the action acts **on**" (the object *instance*), the
  actor is `circumstance_ref` → `user`, so OpenFGA deliberately diverges from the
  Cedar/AuthZEN principal-slotting (in ReBAC the user/object asymmetry is
  load-bearing). Casbin's `dom` (tenancy) is intentionally **not** populated
  (`circumstance_ref` is request context, not a tenant). AuthZEN mirrors Cedar (PARC)
  with a closed entity-kind enum (a deliberate sketch simplification).
- **Executable checks (no prose-only):** per-engine `maps_each_coordinate_to_its_slot`
  (distinct values → exact slots — the faithfulness + anti-aliasing negative control);
  and two contract-level tests in `engine.rs` over all four adapters —
  `every_adapter_is_total_and_deterministic` and `mappings_agree_on_the_verb_slot`
  (the verb lands in each engine's verb slot — the negative control against
  slot-swapping).

**Risk/limitation:** **sketches = request-shape mapping only.** Real engine evaluation
(`Casbin` `enforce`, `OpenFGA` `Check`, AuthZEN PDP round-trip, real `cedar-policy`) is
deferred and feature-gated — it would introduce a *separate* fallible evaluator trait
and optional per-engine deps, never touching `causlane-core` and never replacing
`authz_gate`. No serde (wire formats are part of the deferred evaluation). OpenFGA
actor normalization and `binding`-sidecar enforcement (contextual tuples) are deferred.
Pure additive `causlane-runtime` (no core / codegen / bundle-hash / formal-IR change →
formal contour untouched). Approvals/step-up/SoD/capability/redaction are M06.4–M06.7.
