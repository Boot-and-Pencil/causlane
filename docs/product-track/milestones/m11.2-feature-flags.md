# M11.2 — Feature flags

- **Stage:** S11 — Public pre-alpha/bootstrap and alpha publication
- **Status:** `done_or_near_done`
- **Purpose:** default minimal; existing optional runtime integrations are
  explicit and non-default.

## Required outputs

- normative doc/spec update: crate READMEs and S11 track record the feature
  surface;
- machine-readable artifact: `docs/release/publish-readiness.json`
  `feature_surface`;
- tests/replay/formal receipt: targeted Cargo feature checks listed below;
- coverage/readiness status update: generated publish readiness schema/report
  includes `feature-surface`;
- risk/limitation note: future schema/sqlx/cedar/formal feature flags remain
  deferred until those integrations become optional Cargo surfaces.

## Acceptance checklist

- [x] Implementation or generated artifact exists.
- [x] Docs and ADRs match the implementation.
- [x] No protocol-critical behavior is prose-only.
- [x] Relevant negative controls exist.
- [x] Stale/coverage/release gates updated.

## Evidence

- `tools/publish-readiness --check`
- `python3 tools/validate-json-schema --schema contracts/schema/publish_readiness.schema.json docs/release/publish-readiness.json`
- `./tools/cargo-dev check -p causlane --no-default-features --locked`
- `./tools/cargo-dev check -p causlane-runtime --no-default-features --locked`
- `./tools/cargo-dev check -p causlane-runtime --all-features --locked`
